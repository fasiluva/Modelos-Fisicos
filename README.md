# 📘 Proyecto: Presentación + Programa en Python

Este proyecto está compuesto por dos partes:

1. **Un programa independiente en Python**, ubicado en el archivo `modelos.py`.
2. **Una página web desplegada en Vercel**, que funciona únicamente como presentación del proyecto.

No existe comunicación entre la página y el programa; el archivo `modelos.py` se ejecuta localmente como un script o módulo independiente.

---

## 📂 Estructura del Proyecto

```
...
/public
├── modelos.py          # Programa principal 
├── requirements.txt    # Dependencias para ejecutar el script
```

---

## 🧠 `modelos.py`

`modelos.py` contiene el código principal del proyecto.

### ▶️ Ejecutarlo directamente

```bash
pip install -r requirements.txt

# Si el anterior comando no funciona, probar instalar las dependencias manualmente:

pip install numpy 
pip install matplotlib

# Una vez instaladas las dependencias, ejecutar:

python modelos.py

# Si el anterior comando no funciona, probar 
python3 modelos.py
```




